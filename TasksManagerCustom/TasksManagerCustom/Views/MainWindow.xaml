<Window x:Class="TasksManagerCustom.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:core="clr-namespace:TasksManagerCustom.Core;assembly=TasksManagerCustom.Core"
        Title="{Binding Title}" Height="350" Width="525" >

    <AdornerDecorator>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="90" />
            </Grid.ColumnDefinitions>
            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.TopMenuBar}"
                            Grid.Row="0"
                            Grid.Column="0"
                            Grid.ColumnSpan="2"/>
            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.LeftMenuSpace}"
                            Grid.Row="1"
                            Grid.Column="0" />
            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.ContentRegion}"
                            Grid.Row="1"
                            Grid.Column="1"/>
        </Grid>
    </AdornerDecorator>
</Window>
