namespace TasksManager.Shared.GlobalConstants
{
    // TODO: Review and choose one format in future — DisplayDateTime in XAML uses "dd.MM.yyyy HH:mm" which differs from FullDateTime
    public static class DateFormats
    {
        public const string FullDateTime = "yyyy-MM-dd HH:mm";
        public const string ShortDate = "yyyy-MM-dd";
    }
}
